/**
 * Placeholder: İleride "availability" koleksiyonu (tourId + date bazlı kapasite) eklenebilir.
 * Firestore Locked mode ile tüm erişim server-side (firebase-admin) üzerinden olacak.
 */

export const AVAILABILITY_COLLECTION = 'availability'
